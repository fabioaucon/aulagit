console.log (7/0) // no JS não gera erro de divisãp por zero. Gera infinito

console.log ("10" / 2)  // no JS ele não gera erro de type. Ele vai coverter e realizar a operação se for possível. Se colocar "10,2" ele não vai conseguir identicar como number

console.log (0.1 + 0.7) // operações com ponto flutuante não são precisas no JS

console.log((10.345).toFixed(2))


